# 贡献指南（Contributing）

感谢你关注「职教课程开发专家团」！本仓库以 **MIT License** 发布，欢迎在教学、教研与商业场景下自由学习、复用与二次开发。

## 你可以做什么

- 提 Issue 反馈Bug、措辞或流程问题；
- 提交 PR 增补/修正成员能力、技能引用、知识库内容；
- 基于本包做二次开发（增减成员、扩展 industry-libs 、对接新教学平台）。

## 二次开发与发版纪律（MUST）

发版请严格遵守 **[VERSIONING.md](./VERSIONING.md)**：

1. **判级按「发布范围」而非功能大小**：
   - 仅单专家/局部补丁 → `Z+1`（如 1.3.4 → 1.3.5）；
   - 全体统一发版 → `Y+1`；
   - 红线重大变更 → `X+1`。
2. **四字段同号**：团队整体 + 各成员 `displayName`/`profession` 必须带同一版本号，发版前自查无残留旧号。
3. **嵌入副本同步**：成员技能若内嵌副本（如 `ti-office-docs`），改动须同步到团队包内嵌副本，并走 embed → validate → register 流程。
4. **Changelog**：每次发版在 `VERSIONING.md` 与根 `README.md` 的 Changelog 追加一条记录。

## PR 规范

- 一个 PR 聚焦一件事；描述清楚「改了什么 / 为什么 / 影响面」；
- 涉及能力点编号、课标/整体设计两层分离、命名纪律等红线改动时，请在 PR 说明中标注；
- 合并后由维护者按版本纪律 bump 版本号并打 Tag（如 `v1.4.0`）。

## 许可证

- 二次发布须保留 **MIT 许可证全文**与作者署名（米修老师）及版本号；
- 允许商用与闭源二次分发，作者不承担任何担保责任；商业合作或定制开发请联系 mixstudio@qq.com。
